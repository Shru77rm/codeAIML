import numpy as np
from sklearn.neural_network import MLPClassifier

X = [[0,0],[0,1],[1,0],[1,1]]

y = [0,1,1,0]

mlp = MLPClassifier(
    hidden_layer_sizes=(2,),
    max_iter=1000,
    activation='logistic'
)

mlp.fit(X,y)

print("Predictions:", mlp.predict(X))
